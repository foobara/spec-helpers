RSpec.describe Foobara::SpecHelpers do
  it "has a version number" do
    expect(Foobara::SpecHelpers::VERSION).to_not be_nil
  end

  it "does something useful" do
    expect(false).to be(true)
  end
end
